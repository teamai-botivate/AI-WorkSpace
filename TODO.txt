═══════════════════════════════════════════════════════════
  BOTIVATE AI WORKSPACE — Task Tracker
  Updated: 2026-02-28
═══════════════════════════════════════════════════════════

  For detailed progress: see PROGRESS.md
  For setup instructions: see SETUP.md
  For adding agents: see AGENT_INTEGRATION_GUIDE.md

── PHASE 1: CORE INFRASTRUCTURE (COMPLETED) ────────────

[x] Design config-driven architecture
[x] Create workspace.config.json (single source of truth)
[x] Build Gateway Backend (FastAPI, port 9000)
[x] Build Frontend Shell (React + Vite + Tailwind, port 3000)
[x] Implement dynamic agent card rendering
[x] Implement iframe-based agent loading
[x] Add health monitoring (gateway polls backends)
[x] Build one-click launcher (start-dev.ps1)
[x] Add deployed agent support (Render/cloud)
[x] Set up Git repo + .gitignore
[x] Install all dependencies
[x] Test full end-to-end flow
[x] Write documentation (README, SETUP, INTEGRATION, PROGRESS)

── PHASE 2: AI AGENTS (2/7 COMPLETED) ─────────────────

[x] HR Recruiter & Screener (Resume-Screening-Agent, port 8000)
    → JD Generator, Resume Screening, Aptitude Tests, Gmail
[x] HR Employee Support (HR_Support, deployed on Render)
    → Leave Mgmt, Policy RAG, Approvals, LangGraph Chatbot

── PHASE 2 (REMAINING): BUILD ONE BY ONE ──────────────

[ ] Production, Inventory & Purchase Agent
    → Create folder: agents/production/
    → Backend: FastAPI (port 8002)
    → Frontend: React (port 5176)
    → Features: Order-driven planning, stock alerts, PO management
    → Add entry to workspace.config.json

[ ] Sales, Marketing & Service Agent
    → Create folder: agents/sales/
    → Backend: FastAPI (port 8003)
    → Frontend: React (port 5177)
    → Features: Lead tracking, complaint handling, service tracking
    → Add entry to workspace.config.json

[ ] Store, Maintenance & Repair Agent
    → Create folder: agents/maintenance/
    → Backend: FastAPI (port 8004)
    → Frontend: React (port 5178)
    → Features: Asset tracking, preventive maintenance, spare parts
    → Add entry to workspace.config.json

[ ] AI Operations Monitor + MIS Agent
    → Create folder: agents/operations-mis/
    → Backend: FastAPI (port 8005)
    → Frontend: React (port 5179)
    → Features: Control panel, stage visibility, MIS auto-generation
    → Add entry to workspace.config.json


── PHASE 3: SUPER AGENT ────────────────────────────────

[ ] Build Super Agent (Router / Orchestrator)
    → Create folder: agents/super-agent/
    → Backend: FastAPI + LangGraph (port 8006)
    → This agent calls ALL other agents' APIs
    → Cross-department analysis
    → "What's the biggest problem today?"
    → Immediate action recommendations
    → Add entry to workspace.config.json


── PHASE 4: PRODUCTION READINESS ───────────────────────

[ ] Create docker-compose.yml for all services
[ ] Add Nginx reverse proxy config
[ ] Environment-based config (dev/staging/prod)
[ ] Auth system for main shell (company login)
[ ] Add dark mode toggle (theme from config)
[ ] Add real-time WebSocket health monitoring
[ ] CI/CD pipeline setup


── NOTES ───────────────────────────────────────────────

- Each agent is INDEPENDENT — own backend + frontend
- Adding a new agent = add folder + add config entry
- workspace.config.json is the SINGLE SOURCE OF TRUTH
- No hardcoded ports, names, or URLs anywhere
- Frontend shell reads everything from config dynamically
